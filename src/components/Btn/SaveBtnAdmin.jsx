import { Button } from "@chakra-ui/react";
import React from "react";

function SaveBtnAdmin() {
  return (
    <Button colorScheme="teal" size="lg">
      Save
    </Button>
  );
}

export default SaveBtnAdmin;
